# Migration `20190826173753-init`

This migration has been generated by timsuchanek at 8/26/2019, 5:37:53 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User"("id" text NOT NULL  ,"username" text NOT NULL DEFAULT '' ,PRIMARY KEY ("id"))
;

CREATE TABLE "public"."Post"("id" text NOT NULL  ,"data" text NOT NULL DEFAULT '' ,PRIMARY KEY ("id"))
;

ALTER TABLE "public"."Post" ADD COLUMN "user" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190826173753-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,23 @@
+datasource db {
+  provider = "postgres"
+  url      = env("POSTGRES_URL")
+  default  = true
+}
+
+generator photon {
+  provider  = "photonjs"
+  output    = "@generated/photon"
+  transpile = false
+}
+
+model User {
+  id       String @id @default(uuid())
+  username String
+  posts    Post[]
+}
+
+model Post {
+  id   String @id @default(uuid())
+  data String
+  user User?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190826173753-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190826173753-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
